import { AreaChart, Area, XAxis, YAxis, Tooltip, ResponsiveContainer } from 'recharts';

const dashboard = () => {
    return (
        <div>
            
        </div>
    );
}
 
export default dashboard;